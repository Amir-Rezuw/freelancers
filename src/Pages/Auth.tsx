import { Fragment } from "react";
import AuthContainer from "../Features/Authentication/AuthContainer";

const Auth = ({}) => {
  return (
    <Fragment>
      <AuthContainer />
    </Fragment>
  );
};

export default Auth;
